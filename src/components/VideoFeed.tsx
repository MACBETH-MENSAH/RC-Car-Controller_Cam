import React from 'react';

const VideoFeed = () => {
  // Placeholder for video feed - will be replaced with actual video stream
  return (
    <div className="video-feed bg-gradient-to-b from-background to-secondary">
      <div className="absolute inset-0 flex items-center justify-center text-muted-foreground">
        Connecting to video feed...
      </div>
    </div>
  );
};

export default VideoFeed;